NAME:JOSEPHASBORNDANIEL
REGNO:192324009

#include <stdio.h>
#include <stdlib.h>

struct Node {
    int data;
    struct Node* next;
};

struct Node* reverseList(struct Node* head) {
    // Implementation goes here
}

int main() {
    struct Node* head = /* create linked list here */;

    struct Node* reversedList = reverseList(head);

    /* print reversed list here */

    return 0;
}
